#pragma once

#include "vector_int.hpp"
#include "vector_double.hpp"
#include "vector_float.hpp"

/* ----- Features to add ---------------
* Constructors that takes in different types of Vector and converts them into a different Vector
    * ex1) algebra::Vector<double>(const Vector<int> &vector);
    * ex2) algebra::Vector<float>(const std::vector<double> &vector);
* Make the same for setters for each Vector
    * ex) algebra::Vector<double>::set_vector(const std::vector<float> &vector);
*/